import { CopyAction } from '../../../types/template'
import { ActionRunner } from './index'

export const migrate: ActionRunner<CopyAction> = async (
  action,
  rootFolder,
  parameters,
): Promise<void> => {
  //
}
